<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
             x:Class="Safarion.Views.QrCodePage"
             Title="Digital ID"
             BackgroundColor="{StaticResource Background}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Done" Clicked="OnCloseClicked"/>
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto, *, Auto" Padding="30">

        <StackLayout Grid.Row="0" Spacing="5" HorizontalOptions="Center" Margin="0,20,0,40">
            <Label Text="MEDICAL ID" FontSize="28" FontAttributes="Bold" TextColor="{StaticResource Primary}" HorizontalOptions="Center"/>
            <Label Text="Scan for Emergency Info" FontSize="14" TextColor="{StaticResource SecondaryDarkText}" HorizontalOptions="Center"/>
        </StackLayout>

        <Border Grid.Row="1" StrokeShape="RoundRectangle 20" StrokeThickness="4" Stroke="{StaticResource Primary}" 
                BackgroundColor="White" Padding="20" HorizontalOptions="Center" VerticalOptions="Start"
                HeightRequest="300" WidthRequest="300">

            <zxing:BarcodeGeneratorView x:Name="QrCodeView"
                                        Format="QrCode"
                                        ForegroundColor="Black"
                                        BackgroundColor="White"
                                        HorizontalOptions="Fill" 
                                        VerticalOptions="Fill"/>
        </Border>

        <Label Grid.Row="2" Text="Use this code if you are unable to speak." 
               TextColor="Gray" FontSize="12" HorizontalOptions="Center" Margin="0,20"/>
    </Grid>
</ContentPage>